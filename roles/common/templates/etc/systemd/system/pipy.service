# {{ ansible_managed }}
[Unit]
Description=PiPy Service
After=network.target

[Service]
ExecStart=/usr/local/opt/{{ pypy_distribution }}/bin/pypy -m twisted pipy
ExecReload=/bin/kill -HUP $MAINPID
KillMode=process
Restart=on-failure

[Install]
WantedBy=multi-user.target
